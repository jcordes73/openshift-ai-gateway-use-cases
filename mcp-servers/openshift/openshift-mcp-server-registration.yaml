apiVersion: mcp.kuadrant.io/v1alpha1
kind: MCPServerRegistration
metadata:
  name: openshift-mcp-server
  namespace: llm-hosting
spec:
  prefix: openshift_mcp_server_
  targetRef:
    group: "gateway.networking.k8s.io"
    kind: "HTTPRoute"
    name: "openshift-mcp-server"
    namespace: "llm-hosting"
